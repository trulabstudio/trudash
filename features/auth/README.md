# Auth Feature

Handles Supabase authentication flows for login, password recovery, and sign out.

Business logic lives in `actions/auth.action.ts`.
Validation schemas live in `schemas/auth.schema.ts`.
Reusable auth forms live in `components`.
